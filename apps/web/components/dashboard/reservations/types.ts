/** Display label shown by StatusPill (mapped from the DB enum). */
export type ReservationStatusLabel =
  | "Inquiry"
  | "Pending"
  | "Confirmed"
  | "Checked in"
  | "Checked out"
  | "Cancelled"
  | "No-show";
